<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.blogspot.weargenius.alberto">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACTION_DIAL" />
    <uses-permission android:name="android.permission.camera" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".callInterception.CallDetectService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".callInterception.CallDetails"
            android:label="@string/title_activity_call_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".notes.NotesInterface"
            android:label="@string/title_activity_notes_interface"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".notes.DetailNote"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".expenses.AllExpenses"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity
            android:name=".expenses.new_product"
            android:label="@string/title_activity_new_product"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".expenses.OldProducts"
            android:label="@string/title_activity_old_products"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".smarthome.ControlAppliances"
            android:label="@string/title_activity_control_appliances"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".foods.FoodMenu"
            android:label="@string/title_activity_food_menu"
            android:theme="@style/AppTheme" />

        <service
            android:name=".notifications.NotificationAll"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".notifications.NotificationServiceReceiver"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".mobileMessages.ReadInboxMessages"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".alarmManager.SmartHomeAlarm"
            android:label="@string/title_activity_smart_home_alarm"
            android:theme="@style/AppTheme" />

        <service
            android:name=".location.MyLocationTracker"
            android:enabled="true"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".location.LocationMap"
            android:label="@string/title_activity_location_map" />
        <activity
            android:name=".location.PreviousLocations"
            android:label="@string/title_activity_previous_locations"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
